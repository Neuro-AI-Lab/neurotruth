# Web API and ASGI runtime
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database/project dependencies kept from the original backend scaffold
sqlalchemy==2.0.30
asyncpg==0.29.0
psycopg2-binary==2.9.9
pgvector==0.2.5
pydantic==2.7.0
pydantic-settings==2.2.1
python-dotenv==1.0.1
httpx==0.27.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Feature-based (RandomForest) craving inference. Versions must match the training
# env (feature_classification/requirements.txt) so the saved joblib bundle
# unpickles cleanly. No torch/GPU is used anymore.
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.9.0
neurokit2==0.2.13
joblib==1.5.3
